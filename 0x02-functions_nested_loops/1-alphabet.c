#include "main.h"

/**
 * main - function that prints the alphabet, in lowercase.
 *
 * Return: Always 0.
 */

int main(void)
{

	char a;

	for (a = 'a'; a <= 'z'; a++)
	{
		_putchar(a);
	}
	_putchar('\n');

	return (0);
}
